# 마음결 v4

AI Mental Daily Journal. GitHub `main`에 병합되면 Render Blueprint의 `maeumgyeol-v4` 서비스가 이 폴더를 `rootDir`로 사용합니다.

## Render secrets
- `OPENAI_API_KEY`: OpenAI API key (GitHub에 커밋하지 않음)
- `APP_TOKEN`: 공개 URL 보호용 긴 임의 문자열

기본 모델은 `gpt-5.6-luna`이며 `render.yaml`에서 설정됩니다.
